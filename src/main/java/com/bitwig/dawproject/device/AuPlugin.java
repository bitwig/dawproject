package com.bitwig.dawproject.device;

import jakarta.xml.bind.annotation.XmlRootElement;

/** The Apple AU plug-in format. */
@XmlRootElement(name = "AuPlugin")
public class AuPlugin extends Plugin {
	// Intentionally empty
}
